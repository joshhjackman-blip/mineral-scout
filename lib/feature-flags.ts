/**
 * Product feature toggles.
 * Flip these when re-enabling archived surfaces — keep the code paths.
 */

/** Satellite Imagery / Pad Ops desk — archived 2026-08-04 (too complex for now). */
export const SATELLITE_IMAGERY_ENABLED = false
